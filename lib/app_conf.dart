import 'package:flutter/material.dart';

class FlavorConfig{
  String title;
  String ImageLocation;
  ThemeData theme;

  FlavorConfig({
    required this.title,
    required this.ImageLocation,
    required this.theme
});
}